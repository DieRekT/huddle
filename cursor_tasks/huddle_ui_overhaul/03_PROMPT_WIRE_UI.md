# Cursor Prompt — Task 3: Wire State Machine into UI

Wire the state machine output into the Room Awareness UI.

Requirements:
- UI text must be derived from states only.
- Must respect "no more than 1 UI change per 1.2s".
- Bind coverage chip + reason phrase.
- Enforce attention ceiling when Coverage is Limited.

Output:
- Code changes + brief verification notes
- If you add logging, keep it behind Debug only
