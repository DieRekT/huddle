# Cursor Prompt — Task 5: Settings Wiring

Wire only these settings (no extras):
- Alerts: Off / Gentle / Strong
- Sensitivity: Low / Normal / High
- Context: Minimal / Normal / Extra reassurance
- Accessibility: Text size, Contrast, Reduce motion

Requirements:
- Defaults safe (Gentle alerts is acceptable).
- Contrast + Reduce motion must visibly work.
- Text size must not cause clipping.

Output:
- Settings storage approach (localStorage ok)
- UI wiring + tests where feasible
